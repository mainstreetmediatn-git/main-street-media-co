# Main Street Media Co.

React and Vite website for Main Street Media Co., including the WebAuthority article hub.

## Development

```bash
npm install
npm run dev
```

The visibility audit form posts JSON to `POST /api/audit-request`. The deployment environment must provide that endpoint.
